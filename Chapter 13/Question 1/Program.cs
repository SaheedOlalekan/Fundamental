using System;

namespace Question_1
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");

                // 1. Describe the strings in C#. What is typical for the string type? 
                // Explain which the most important methods of the string class are.

               /* Stirng is an object of type string whose value is text. A string is a
               sequence of characters stored in a certain address in memory. 
               Char is a typecal of string type. Among the methods in string are : Concat (); Substring ();
               IndexOf (); LastIndexOf ();





               */
        }
    }
}
