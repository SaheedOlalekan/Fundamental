using System;

namespace Question_13
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }

            // 13. * Write a method that calculates the product of two polynomials with 
            // integer coefficients, for example (3x2 + x - 3) * (x - 1) = (3x3 -
            // 2x2 - 4x + 3).

        static void ProductOfTwoPolynomials( int[]myArray, int[] myNunbers)
        {
            int a = 0, b = 0, c = 0, d = 0, x = 0, k1 = 0, k2 = 0,result = 0;
           // double x = 0;
            a = myArray[0] * x * x * x;
            b = myArray[1] * x * x;
            c = 2 * x * x;
            d = k1 * k2;
        }
    }
}
