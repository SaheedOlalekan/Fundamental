using System;

namespace Question_3
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");

                // 3. Add a static field for the class Student, which holds the number of 
                // created objects of this class.

                
                // Student student = new Student();
                // student.Age = 15;
                // student.Universities = University.CONVENANT;
                // student.FullNames = "Ajanlekoko Ojuolape";
                // int? studentAge = student.Age;
                // System.Console.WriteLine("The student university is: " + student.Universities);
                // System.Console.WriteLine( "The student university enum number is: " + (int)student.Universities);
                // System.Console.WriteLine($"{student.FullNames} is {studentAge} years old student of {student.Universities} university.");
                // System.Console.WriteLine(studentAge is 15);
                // System.Console.WriteLine(studentAge is 25);
        }
    }
}
